# Home Assistant itself already provides aiohttp at runtime. This file only
# documents the dependency for local development outside a real Home
# Assistant environment (e.g. to run the unit tests in tests/).
aiohttp>=3.9
