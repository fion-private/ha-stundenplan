-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
# aioresponses (upstream) is incompatible with aiohttp>=3.14 - it constructs
# ClientResponse without the now-required `stream_writer` kwarg, causing
# every mocked request to fail with a TypeError. aioresponses-ng is a
# maintained fork with the fix (same import name: `import aioresponses`).
# See: https://github.com/pnuckowski/aioresponses/pull/288
aioresponses-ng>=0.7.9
freezegun>=1.5

# Only needed to import custom_components/stundenplan/coordinator.py and
# __init__.py (ConfigEntry, DataUpdateCoordinator, ...). The tests do NOT
# instantiate a full, running Home Assistant core for this.
homeassistant>=2024.6

ruff>=0.6
mypy>=1.11
